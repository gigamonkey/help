# Whitelist style: exclude everything, then re-include only what the image
# needs. This makes .env / fly.env / *.db leaks structurally impossible.
*

!Dockerfile
!package.json
!package-lock.json
!index.ts
!modules
!public
!views
!run.sh
!litestream.yml
